const INITIAL_STATE_USER = {
  user: {
    email: '',
    senha,
  },
};

const userReducer = (state = INITIAL_STATE_USER) => {
  switch (action.type) {
  case value:
  default:
    return state;
  }
};

export default userReducer;
