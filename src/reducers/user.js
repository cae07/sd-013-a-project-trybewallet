const INITIAL_STATE_USER = {
  email: '',
};

const user = (state = INITIAL_STATE_USER, action) => {
  switch (action.type) {
  case '':
  default:
    return state;
  }
};

// uma alteração qualquer

export default user;

