const INITIAL_STATE_WALLET = {
  currencies: [],
  expenses: [],
};

const wallet = (state = INITIAL_STATE_WALLET) => {
  switch (action.type) {
  case value:
  default:
    return state;
  }
};

export default wallet;
